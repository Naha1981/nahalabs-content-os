# SQLite database (Prisma; path is relative to prisma/schema.prisma)
DATABASE_URL="file:../db/custom.db"

# Optional: route image generation to your own GPU-box gateway
# (ComfyUI / Wan / HunyuanVideo wrapper). Contract: POST {prompt,size} -> {image_base64}
# VIDEO_FACTORY_IMAGE_ENDPOINT=http://your-gpu-box:8188/video-factory/image
