# Public HTTPS URL of the Reactivate FastAPI service.
REACTIVATE_API_URL=https://your-reactivate-api.example.com

# Optional shared bearer token. Configure the same token on the FastAPI service before enabling it.
REACTIVATE_API_TOKEN=
