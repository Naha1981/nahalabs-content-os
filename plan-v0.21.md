# v0.21 plan
1. Add local FFmpeg assembly service.
2. Add prospect-scoped assembly endpoint.
3. Validate rendered local media before assembly.
4. Add regression tests.
5. Add UI controls to select rendered jobs and assemble.
6. Run backend + media verification; do not claim browser QA without a browser runtime.
