# Reactivate v0.38.3

Windows local-browser connectivity hotfix.

Frontend: `http://localhost:5173/`
Backend: `http://127.0.0.1:8000`

The frontend explicitly targets IPv4 loopback to match the Uvicorn bind address.
